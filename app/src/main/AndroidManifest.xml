<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.halodoc.halodoc">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Halodoc2">
        <activity android:name=".ui.home.buatjanji.HospitalPaymentActivity"></activity>
        <activity android:name=".ui.home.buatjanji.HospitalDetailActivity" />
        <activity android:name=".ui.home.buatjanji.HospitalActivity" />
        <activity android:name=".ui.home.lainnya.MenuLainnyaActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>